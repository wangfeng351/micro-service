server:
  # 80默认端口，访问时无需带端口号
  port: 8000

info:
  app:
    name: consumer-hello111

spring:
  application:
    name: consumer-hello

eureka:
  instance:
    hostname: wf.com
    prefer-ip-address: true

  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:7001/eureka/
